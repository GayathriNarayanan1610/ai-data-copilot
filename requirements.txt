# Runtime dependencies for the Streamlit UI + copilot backend.
# Mirrors [project.dependencies] + the `ui` and `llm` extras in pyproject.toml.
# (For development, prefer:  pip install -e ".[dev,ui,llm]")

# --- core backend ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
pandas>=2.1

# --- UI ---
streamlit>=1.36

# --- model backends (needed for LLM_MODE=ollama|gemini) ---
langchain-core>=0.2
langchain-ollama>=0.1
langchain-google-genai>=1.0
